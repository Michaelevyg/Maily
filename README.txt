in order to work be able to webhook the responses 'yes' or 'no' from SendGrid (mail provider):
1. go to https://app.sendgrid.com/settings/mail_settings
2. change mail URL to https://mymaily.localtunnel.me/api/surveys/webhooks

1. go to surveyTemplate.js
2. change api/surveys to /api/surveys